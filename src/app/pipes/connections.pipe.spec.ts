import { ConnectionsPipe } from './connections.pipe';

describe('ConnectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConnectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
